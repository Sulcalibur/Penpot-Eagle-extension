import { definePlugin } from '@penpot/plugin-types'

export default definePlugin({
  name: 'eagle-assets',
  register: (api) => {
    // Your plugin initialization code here
    console.log('Eagle plugin initialized')
  }
}) 